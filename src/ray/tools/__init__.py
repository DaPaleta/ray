"""Ray's typed tools. Every query lives in this package (IR2).

A tool retrieves and cites. A tool never reasons — reasoning belongs to a subagent
(IR7, ADR-008). Nine tools in total; plan.md section 4.4 lists them.
"""
